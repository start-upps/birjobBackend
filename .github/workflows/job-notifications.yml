name: Job Notifications

on:
  # Run every 40 minutes
  schedule:
    - cron: '*/45 * * * *'
  
  # Allow manual trigger for testing
  workflow_dispatch:

jobs:
  send-job-notifications:
    runs-on: ubuntu-latest
    
    steps:
    - name: Health Check
      run: |
        echo "🏥 Checking backend health..."
        HEALTH_RESPONSE=$(curl -s "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/health")
        STATUS=$(echo "$HEALTH_RESPONSE" | jq -r '.status // "unknown"')
        
        if [ "$STATUS" = "healthy" ]; then
          echo "✅ Backend is healthy"
        else
          echo "❌ Backend health check failed: $STATUS"
          exit 1
        fi
        
        # Show metrics
        echo "$HEALTH_RESPONSE" | jq -r '.metrics // {} | to_entries[] | "- \(.key): \(.value)"'
    
    - name: Check Active Users
      run: |
        echo "👤 Checking active users with keywords..."
        
        # Retry logic for deployment timing
        for i in {1..3}; do
          echo "⏳ Attempt $i: Checking active devices..."
          
          # Get list of devices for verification
          DEVICES_RESPONSE=$(curl -s "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/notifications/devices")
          DEVICE_COUNT=$(echo "$DEVICES_RESPONSE" | jq -r '.data.active_devices_count // 0')
          
          echo "Active devices found: $DEVICE_COUNT"
          
          if [ "$DEVICE_COUNT" -gt 0 ]; then
            echo "✅ SUCCESS: Found $DEVICE_COUNT active devices"
            
            # Show device info
            echo "$DEVICES_RESPONSE" | jq -r '.data.devices[] | "Device: \(.device_id) | Keywords: \(.keywords_count) | Preview: \(.keywords | join(", "))"'
            break
          else
            echo "⚠️ No devices found on attempt $i"
            if [ "$i" -eq 3 ]; then
              echo "❌ Failed after 3 attempts!"
              exit 1
            fi
            echo "⏳ Waiting 30s before retry..."
            sleep 30
          fi
        done
    
    - name: Process Job Notifications
      run: |
        echo "🔍 Scanning database for job matches every 45 minutes..."
        echo "💡 Database uses truncate/load logic - refreshes hourly with latest job market snapshot"
        
        # First, get total count of jobs
        echo "📊 Fetching job count from database..."
        
        # Get total count first (all jobs - database uses truncate/load logic)
        TOTAL_RESPONSE=$(curl -s "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/jobs/?limit=1&offset=0")
        TOTAL_JOBS=$(echo "$TOTAL_RESPONSE" | jq -r '.data.pagination.total // 0')
        
        echo "📈 Total jobs in database (current snapshot): $TOTAL_JOBS"
        
        if [ "$TOTAL_JOBS" -eq 0 ]; then
          echo "ℹ️ No jobs found in database"
          exit 0
        fi
        
        # Fetch jobs in batches (API limit is 100 per request)
        echo "📦 Fetching jobs in batches (API limit: 100 per request)..."
        
        BATCH_SIZE=100
        ALL_JOBS="[]"
        OFFSET=0
        
        while [ "$OFFSET" -lt "$TOTAL_JOBS" ]; do
          echo "📥 Fetching batch: offset=$OFFSET, limit=$BATCH_SIZE"
          
          BATCH_RESPONSE=$(curl -s "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/jobs/?limit=$BATCH_SIZE&offset=$OFFSET")
          BATCH_JOBS=$(echo "$BATCH_RESPONSE" | jq -c '.data.jobs // []')
          BATCH_COUNT=$(echo "$BATCH_JOBS" | jq 'length')
          
          echo "📊 Batch size: $BATCH_COUNT jobs"
          
          if [ "$BATCH_COUNT" -eq 0 ]; then
            echo "⚠️ Empty batch received, stopping"
            break
          fi
          
          # Merge batch with all jobs
          ALL_JOBS=$(echo "$ALL_JOBS $BATCH_JOBS" | jq -s 'add')
          
          # Update offset
          OFFSET=$((OFFSET + BATCH_SIZE))
          
          # Small delay to avoid rate limiting
          sleep 1
        done
        
        # Get final count
        JOBS_COUNT=$(echo "$ALL_JOBS" | jq 'length')
        echo "📈 Successfully fetched $JOBS_COUNT total jobs to process"
        
        if [ "$JOBS_COUNT" -eq 0 ]; then
          echo "ℹ️ No jobs retrieved from batches"
          exit 0
        fi
        
        # Process job matches efficiently using grep
        echo "🔔 Processing job matches efficiently with grep-based matching..."
        
        # Get active devices with keywords
        DEVICES_RESPONSE=$(curl -s "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/notifications/devices")
        DEVICES=$(echo "$DEVICES_RESPONSE" | jq -c '.data.devices // []')
        DEVICE_COUNT=$(echo "$DEVICES" | jq 'length')
        
        echo "👥 Found $DEVICE_COUNT active devices to check"
        
        TOTAL_PROCESSED=$JOBS_COUNT
        TOTAL_MATCHED=0
        TOTAL_SENT=0
        
        # Create searchable job text file
        echo "📋 Creating searchable job database..."
        echo "$ALL_JOBS" | jq -r '.[] | "\(.id)|\(.title // "")|\(.company // "")"' | tr '[:upper:]' '[:lower:]' > /tmp/jobs.txt
        
        # Process each device
        for device_index in $(seq 0 $((DEVICE_COUNT - 1))); do
          DEVICE=$(echo "$DEVICES" | jq -c ".[$device_index]")
          DEVICE_ID=$(echo "$DEVICE" | jq -r '.device_id')
          DEVICE_TOKEN=$(echo "$DEVICE" | jq -r '.device_token // ""')
          KEYWORDS=$(echo "$DEVICE" | jq -r '.keywords[]' | tr '[:upper:]' '[:lower:]' | tr '\n' '|')
          
          echo "🔍 Checking device ${DEVICE_ID:0:8}... with keywords: $KEYWORDS"
          
          if [[ -n "$KEYWORDS" ]]; then
            # Use grep to find matching jobs efficiently
            MATCHES=$(grep -E "$KEYWORDS" /tmp/jobs.txt || true)
            MATCH_COUNT=$(echo "$MATCHES" | wc -l)
            
            if [[ -n "$MATCHES" && "$MATCHES" != "" ]]; then
              echo "✅ Found $MATCH_COUNT job matches for device ${DEVICE_ID:0:8}..."
              
              # Send test notification for this device
              echo "📱 Sending test notification to device..."
              NOTIF_RESPONSE=$(curl -s -X POST "${{ secrets.BACKEND_URL || 'https://birjobbackend-ir3e.onrender.com' }}/api/v1/notifications/test/$DEVICE_TOKEN")
              
              NOTIF_SUCCESS=$(echo "$NOTIF_RESPONSE" | jq -r '.success // false')
              
              if [[ "$NOTIF_SUCCESS" == "true" ]]; then
                TOTAL_SENT=$((TOTAL_SENT + 1))
                echo "📱 ✅ Notification sent successfully to device ${DEVICE_ID:0:8}..."
              else
                echo "❌ Notification failed: $(echo "$NOTIF_RESPONSE" | jq -r '.message // "Unknown error"')"
              fi
              
              TOTAL_MATCHED=$((TOTAL_MATCHED + 1))
              
              # Show first few matches as samples
              echo "📊 Sample matches:"
              echo "$MATCHES" | head -5 | while IFS='|' read job_id job_title job_company; do
                echo "  - Job: $job_title | Company: $job_company"
              done
            else
              echo "ℹ️ No matches found for device ${DEVICE_ID:0:8}..."
            fi
          fi
        done
        
        # Cleanup
        rm -f /tmp/jobs.txt
        
        # Set final results
        JOBS_PROCESSED=$TOTAL_PROCESSED
        DEVICES_MATCHED=$TOTAL_MATCHED
        NOTIFICATIONS_SENT=$TOTAL_SENT
        ERRORS=0
        SUCCESS=true
        
        echo "📊 Notification Results:"
        echo "- Jobs processed: $JOBS_PROCESSED"
        echo "- Devices matched: $DEVICES_MATCHED" 
        echo "- Notifications sent: $NOTIFICATIONS_SENT"
        echo "- Errors: $ERRORS"
        echo "- Success: $SUCCESS"
        
        # Show success/failure
        if [ "$NOTIFICATIONS_SENT" -gt 0 ]; then
          echo "✅ SUCCESS: $NOTIFICATIONS_SENT notifications sent!"
        elif [ "$DEVICES_MATCHED" -gt 0 ]; then
          echo "⚠️ Devices matched but no notifications sent (check APNs configuration)"
        else
          echo "ℹ️ No matching jobs found for current user keywords"
        fi
        
        # Log timestamp for monitoring
        echo "🕐 Completed at: $(date)"
        
        # Force exit to ensure GitHub Action terminates
        exit 0